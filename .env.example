# The application reads these values directly from the process environment.
# This file is documentation only; it is not loaded automatically.
PORT=8080
DATABASE_URL=poolclub.db
SESSION_TTL=24h
SESSION_COOKIE_SECURE=false
